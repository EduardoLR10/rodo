// use crate::commands::parser;
// extern crate walkdir;
// use walkdir::WalkDir;
// use std::fs::File;
// use std::io::{ self, BufRead};
// use std::str;

pub fn catalog_path(_directory_path: &str) -> Result<Vec<String>, ()> {
    todo!("catalog_path is yet not implemented")
}
